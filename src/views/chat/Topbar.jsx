import React, { useState } from 'react';
import styles from './chat.module.css';

const Topbar = ({chats, onSelectChat }) => {
  

  return (
    <div className={styles.topbar}>
          TOPBAR
    </div>
  );
};

export default Topbar;